#CATATAN

#Ketika suatu string dikenakan fungsi tuple atau suatu fungsi string kita konversikan menjadi sebuah tuple, maka akan dihasilkan
                          suatu tuple yang elemennya terdiri dari karakter-karakter yang membentuk string tersebut.

#Prefix: string yang berada di depan suatu string tertentu.
#Suffix: sekumpulan string yang berada di bagian belakang.



#OR atau operator logika | digunakan untuk menseleksi baris-baris di suatu kolom yang nilai-nilainya sesuai dengan apa yang kita mau.
#Fungsi isin() memiliki fungsi yang sama dengan OR atau operator logika |
#Operator negasi (~) digunakan untuk menseleksi baris-baris di suatu kolom yang nilai-nilainya tidak sesuai dengan apa yang diminta.



#dtypes digunakan untuk mengetahui jenis tipe data.
#Pada dtype, string dikenali sebagai object.
#astype() digunakan untuk mengkonversi tipe data ke tipe data numerik yang berada dalam satu kolom.
#Untuk mengkonversi tipe data ke tipe data numerik untuk keseluruhan kolom dalam Data Frame, kita dapat menggunakan to_numeric() .
#coerce digunakan jika pada saat proses konversi tipe data ke numerik terdapat suatu data yang memang tidak dapat kita konversikan
ke numerik, maka data tersebut akan digantikan dengan NaN (Not a Number atau Not a Value atau bisa dianggap juga sebagai missing value).



#makeDataIndex() digunakan untuk men-generate data yang berisi tanggal dan jam.
#reset_index() digunakan untuk generate index baru yang berupa index yang berupa Integer (0,1,2,3,4), sehingga nantinya kolom index akan
dipandang atau diadopsi sebagai kolom biasa.

#Kita dapat memilih kolom bertipe data numerik dengan menggunakan:
#df.select_dtypes(include='number') -> fungsi ini akan memanggil atau memilih kolom-kolom yang bertipe data numerik saja.
#df.select_dtypes(include='float') -> fungsi ini akan memanggil atau memilih kolom-kolom yang bertipe data float saja.
#df.select_dtypes(include='int') -> fungsi ini akan memanggil atau memilih kolom-kolom yang bertipe data Integer saja.

#Kita juga dapat memilih kolom bertipe data string atau object dengan menggunakan:
#df.select_dtypes(include='object') -> fungsi ini akan memanggil atau memilih kolom-kolom yang bertipe data string saja.

#Kita juga dapat memilih kolom bertipe data datetime dengan menggunakan:
#df.select_dtypes(include='datetime') -> fungsi ini akan memanggil atau memilih kolom-kolom yang bertipe data datetime saja.

#Kita dapat juga memilih kolom dengan kombinasi tipe data dengan menggunakan:
#df.select_dtypes(include=['number','object']) -> fungsi ini akan memanggil atau memilih kolom-kolom yang bertipe data numerik atau kolom-kolom yang
bertipe data string saja.


